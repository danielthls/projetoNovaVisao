program NovaVisao2;

uses
  Vcl.Forms,
  uDao.Cliente in 'uDao.Cliente.pas' {Form2},
  uEntity.Cliente in 'uEntity.Cliente.pas',
  UUtils.Banco in 'UUtils.Banco.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TForm2, Form2);
  Application.Run;
end.
